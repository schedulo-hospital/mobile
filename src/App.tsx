import React from 'react'
import {SafeAreaView, Text} from 'react-native'

const App = () => {
  return (
    <SafeAreaView>
      <Text style={{marginTop: 40, textAlign: 'center', fontSize: 20, fontWeight: 'bold'}}>Schedulo</Text>
    </SafeAreaView>
  )
}

export default App
